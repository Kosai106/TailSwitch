#pragma once

#include <QIcon>

// Returns the embedded, tightly cropped Tailscale mark tinted to the current
// application palette so it remains legible in light and dark Plasma themes.
QIcon tailscaleLogoIcon();
